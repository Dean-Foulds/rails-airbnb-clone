 <% if @home.pictures.first
  main_home_picture = cl_image_path @home.pictures.first.path
else
  main_home_picture = ""
  end %>

  <div class="row">
    <!-- PROPERTY IMAGE -->
    <div class="col-xs-12">
      <div class="col-xs-4">
       <div class="featurette-img-sm home-image-thumbnail" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.0) 100%, rgba(0,36,61,0.0) 100%), url('<%= main_home_picture %>') ;">
       </div>

       <!-- BOOKING FORM -->
       <div class="text-left">
        <% if current_user %>
        <div class="form-wrapper">
          <%= render 'homes/booking_form' %>
        </div>
        <% else %>
        <p>Log in to arrange a viewing.</p>
        <% end %>
      </div>
    </div> <!-- end of left col -->
    <div class="col-xs-8">

      <!-- PROPERTY DETAILS -->

      <h2 class="featurette-heading"> <%= @home.address %></h2>
        <h3>  <%= @home.post_code %> </h3>


      <p>Donec ullamcorper nulla non metus auctor fringilla. Vestibulum id ligula porta felis euismod semper. Praesent commodo cursus magna, vel scelerisque nisl consectetur. Fusce dapibus, tellus ac cursus commodo.</p> <br>

      <div class="text-left">
        <% if policy(@home).update? %>
          <%= link_to "Edit", edit_home_path(@home), :class => "btn btn-success" %>
        <% end %>

        <% if policy(@home).destroy? %>
          <%= link_to "Delete", home_path(@home), :class => "btn btn-default",
             method: :delete,
             data: { confirm: "Are you sure?" } %>
        <% end %>
     </div>

      <div class="status">

        <% if @home.home_status == "" %>
          No status
        <% else %>
          <div class="logo-block">
            <i class="fa fa-key" aria-hidden="true"></i>
            <p> <%= @home.home_status %></p>
          </div>
        <% end %>

        <% if @home.room_type == "" %> <br>
          No status
        <% else %>
          <div class="logo-block">
            <i class="fa fa-home" aria-hidden="true"></i>
            <p> <%= @home.room_type %>  </p>
          </div>
        <% end %>

        <% if @home.number_of_rooms == "" %> <br>
          No status
        <% else %>
          <div class="logo-block">
          <i class="fa fa-bed" aria-hidden="true"></i>
          <p> <%= @home.number_of_rooms %>  </p>
          </div>
        <% end %>
      </div>

   </div> <!-- end of right col -->
 </div> <!-- end of col 12 -->
</div>



<%= content_for(:after_js) do %>
<script>
 $(function(){
   $('.datepicker').datepicker({
     format: 'dd-mm-yyyy',
     startDate: '+0d',
     autoclose: true
   });
 })
</script>
<% end %>
<!-- SCRIPT end -->
