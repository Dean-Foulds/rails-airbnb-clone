 <% if @home.pictures.first
  main_home_picture = cl_image_path @home.pictures.first.path
else
  main_home_picture = ""
  end %>

<div class="row">
  <!-- PROPERTY IMAGE -->
  <div class="col-xs-12">

    <div class="col-xs-4">
       <div class="featurette-img-sm home-image-thumbnail" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.0) 100%, rgba(0,36,61,0.0) 100%), url('<%= main_home_picture %>') ;">
      </div>
        <!-- BOOKING FORM -->
        <div class="text-left">
          <% if current_user %>
          <div class="form-wrapper">
            <%= render 'homes/booking_form' %>
          </div>
          <% else %>
          <p>Log in to arrange a viewing.</p>
          <% end %>
        </div>
      </div> <!-- end of left col -->



  <div class="col-xs-8">

  <!-- PROPERTY DETAILS -->


  <h2 class="featurette-heading"> <%= @home.address %></h2>
  <p class="text-info">  <%= @home.post_code %> </p>
  <div class="status">
    <% if @home.status == "" %>
    No status
    <% else %>
    <%= @home.status %>
    <% end %>
  </div>

    <p>Donec ullamcorper nulla non metus auctor fringilla. Vestibulum id ligula porta felis euismod semper. Praesent commodo cursus magna, vel scelerisque nisl consectetur. Fusce dapibus, tellus ac cursus commodo.</p> <br>

    <div class="text-left">
       <% if current_user %>
       <%= link_to "Edit ", edit_home_path(@home), :class => "btn btn-primary btn-sm" %>
       <%= link_to "Delete", home_path(@home), :class => "btn btn-default btn-sm",
       method: :delete,
       data: { confirm: "Are you sure?" } %>
       <% end %>
     </div>
    </div> <!-- end of right col -->
  </div> <!-- end of col 12 -->
</div> <!-- end of banner -->



 <%= content_for(:after_js) do %>
 <script>
   $(function(){
     $('.datepicker').datepicker({
       format: 'dd-mm-yyyy',
       startDate: '+0d',
       autoclose: true
     });
   })
 </script>
 <% end %>
 <!-- SCRIPT end -->
