
<div class="booking-wrapper">
<div class="booking-img">
    <div class="container">
      <div class="row">
        <h3>Bookings:</h3>
        <div class="container">
          <div class="row">
            <div class="col-sm-6 col-lg-3 ">
              <p><%= @booking.starts_at %> at <%= @booking.starts_at_time.strftime('%R') %></p>
              <p><%= @booking.home.address %></p>
            </div>
            <%= link_to "Back", booking_path, class: "btn btn-default" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
