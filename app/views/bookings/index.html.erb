<!--   <div class="container">
    <div class="row">
      <div class="col">
      <h3>Viewing appointments:</h3>
        <div>
          <% @homes.each do |home| %>
          <% @bookings.each do |booking| %>
              <%= booking.starts_at %> at <%= booking.starts_at_time.strftime('%R') %>
              <%= booking.home.address %>
        </div>
        <% end %>

      </div>
      <% end %>

        <%= link_to "Back", homes_path, class: "btn btn-default" %>
      </div>
    </div>
  </div>
 -->
